#include <iostream>
#include <vector>

using namespace std;

int solution(std::vector<int> &A) {
    int N = A.size();

    for (int i=0; i<N; i++) {
    }

    return 0;
}

int main()
{
std::vector<int> A = {0,1,0,1,1};
auto res = solution(A);
std::cout << res << " " << std::endl;

        return 0;
}